function itemModel(itemCode,itemName,quantity,price){
    var mainItem = {
        itmCode : itemCode,
        itmName : itemName,
        itmQty : quantity,
        itmPrice : price
    }
    itemAr.push(mainItem);
}